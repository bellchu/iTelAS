# Compatibility evidence

Compatibility claims are recorded only after a protocol fixture or a live host run proves them. Credentials and host screen contents must never be committed as fixtures.

## PUB400 live probe — 2026-07-26

Environment:

- Public PUB400 IBM i service
- TLS port 992
- Native arm64 iTelAS core on macOS
- IBM-3179-2, 24×80, local CCSID 37
- Random ten-character test device name

Verified in a live, read-only connection:

- macOS TLS trust and TCP connection
- Telnet TERMINAL-TYPE, BINARY, EOR, and NEW-ENVIRON negotiation
- RFC 4777 environment variables sent before transparent mode became ready
- RFC 4777 startup confirmation parsed with a successful response code
- TN5250 general data-stream records received and parsed
- Host sign-on screen rendered with at least two editable fields
- Input cursor normalized into an editable field when the host omitted an explicit cursor order

Verified separately in a user-authorized native UI session:

- User-profile and protected password fields accepted by the 5250 input encoder
- Tab and Shift-Tab moved between host input fields; Option-I toggled insert mode
- The key-map panel exposed the Mac-to-5250 mapping, including F13–F24 and Roll Up/Down
- Clipboard paste opened a review surface before insertion and did not send automatically
- IBM i expired-password notice and Change Password workflow rendered correctly
- Current, new, and verification password fields submitted as non-display fields
- Successful forced password change followed by entry to the IBM i Main Menu
- Manual Disconnect ended the transport, locked input, and preserved the last screen as read-only

During that manual verification, credential values were held only in local process
memory and were not added to test fixtures, application profiles, screenshots, logs,
or AI context. After verification, the operator explicitly requested that the current
development credential be retained in the ignored local file
`devenv.secrets.json`. That file is mode `0600`, matches the repository's
`*.secrets.json` ignore rule, and is never loaded by routine or read-only probes.

Not exercised:

- Authenticated commands or business-data access beyond reaching the Main Menu
- End-to-end automatic recovery after an unexpected transport failure
- Printer sessions
- DBCS and bidirectional display behavior

The opt-in test is `PUB400IntegrationTests.testPUB400TLSNegotiationReachesSignOnScreen`. It extracts only the PUB400 hostname from a locally supplied `.eml` file; it does not parse, submit, print, or persist the credential fields.

```sh
ITELAS_PUB400_EML="/path/to/PUB400 profile.eml" \
swift test --disable-sandbox \
  --filter PUB400IntegrationTests/testPUB400TLSNegotiationReachesSignOnScreen
```

The routine test run skips this test unless `ITELAS_PUB400_EML` is supplied.

## Local concurrent session deck evidence

The terminal workspace now supports multiple simultaneously open sessions, including more than one terminal from the same saved profile. Each tab owns a separate screen, history, insert/overwrite mode, startup receipt, reconnect counter, generation token, client, and recovery task. Inactive sessions continue to receive only their own generation-bound events; selecting a tab changes the visible projection without replacing a transport. Manual disconnect retains one locked screen, close removes only the selected runtime, and Connection Studio tests use an isolated short-lived client rather than the terminal deck.

Five app-target tests prove profile-specific 24×80 and 27×132 geometry, retained-screen labels, lifecycle activity classification, independent state/history/edit modes for same-profile sessions, wraparound deck navigation, immediate one-shot alarm signaling with recovery reset, selected-session projection, and deterministic adjacent-tab selection after close. Pencil frame `ru5TF` reports no layout problems and its exported 2× preview is `Design/Previews/ru5TF.png`; the implemented SwiftUI session bar follows its active, reconnecting, retained, add, close, and aggregate-status hierarchy with original drawn glyphs. No host connection, credential read, API-key read, AI request, or PUB400 authentication was performed for this evidence.

## Local function-key layout evidence

The session profile now retains one validated binding for each physical F1–F24 slot, where F13–F24 remain Shift-F1–Shift-F12 on Mac keyboards. Every binding records its target host F-key AID, visible label, and terminal-dock pin independently. Legacy profiles decode to the identity map; duplicate, missing, out-of-range, overlong, or control-bearing bindings fail validation. Hardware keys and the dock resolve the same active profile mapping before entering the existing AID path, so the target host function still controls PF field-data inclusion and screen validation.

Four core tests cover defaults, validation, legacy migration, and customized JSON round trips; one app test covers active-profile resolution and the native 1,080×720 editor render. Pencil frame `eyEAi` reports no layout problems and its exported 2× preview is `Design/Previews/eyEAi.png`; the native render was inspected with all 24 slots, selected-route evidence, explicit host action, dock state, transport boundary, and footer actions visible without clipping. Editing or saving a layout is local profile state and performs no host connection, AID send, credential read, or AI request.

## Local 5250 display-control evidence

Thirty-six focused parser fixtures now cover exact 24×80 and 27×132 clears; Write-to-Display address reset and first/second control timing; host-owned keyboard lock and Invite recovery; host selection of Read Input, Read MDT, and Read MDT Alternate; Start-of-Header resequencing state and PF24-through-PF1 data-exclusion switches; persistent Home/Insert Cursor, transient Move Cursor, default, and no-cursor-move behavior; MDT reset and modified-field nulling; strict display addresses and non-wrapping Repeat-to-Address including the final screen cell; bounded all-or-nothing transparent data; `X'80nn'` resequencing and `X'84xx'` transparent-field decoding; raw-byte preservation; hidden and controller-supplied ending attribute positions; row-one/column-zero field starts; field redefinition length preservation and replacement-FCW suppression; all eight documented FFW input types and core flags; preservation of host nulls versus EBCDIC blanks; the complete documented color-display attribute table; persistent Write Extended Attribute primary/color planes, precedence, null continuation, normal reset, non-consuming address behavior, high intensity, blink, underline, reverse, column separators, and the complete `0x80...0x8F` color-code range mapped to a seven-color-plus-background workstation palette; inclusive type-selective Erase-to-Address behavior and address advancement; malformed-order refusal without partial screen mutation; non-display input protection after display erasure; message-light and repeated audible-alarm state; and backward-compatible cursor and terminal-attribute decoding. Twenty editing fixtures cover unified character-type and monocase enforcement across overwrite, insert, paste, and command staging; ordinary full-field advance; auto-enter outcomes; field-exit-required state; mandatory-entry/fill rejection without partial mutation; masked-PF validation bypass; right zero adjustment; signed exit keys; and host-authorized Dup. Eleven input-encoding fixtures prove standard and alternate Read-MDT null/trailing-null behavior, Read Input's global master-MDT and delimiter-free contract, unformatted transparent Read Input, inbound Transparent Data framing for transparent MDT fields, real trailing-blank preservation, masked/non-data AID field-data suppression, bypass-aware resequencing with output-field exclusion, global-master-MDT behavior under a partial chain, fail-closed malformed chain handling, Dup `X'1C'`, signed-numeric D-zoning, and fail-closed signed-plus-transparent behavior. Two Query fixtures prove exact-command recognition and byte-accurate 3179-2/3477-FC identity, geometry, color, and capability replies without PA, Cursor Select, DBCS, or graphics overclaiming. The native Canvas renders cursor/text blink, high intensity, extended colors, column separators, and the active host read mode, while each host alarm event reaches AppKit once. PNG and PDF/print artifacts use the same effective presentation attributes and always suppress non-display content.

These are deterministic local fixtures derived from RFC 1205 and IBM's published 5250 data-stream contract. Core FFW auto-enter, field-exit-required, mandatory-entry/fill, right blank/zero adjustment, signed numeric, Dup, transparent input, SOH PF masks, and read-resequencing behaviors now execute locally through typed outcomes; malformed chains and failed field encoding cannot send a partial input payload. Bulk paste and command staging still never auto-submit. The current SBCS presentation layer intentionally rejects extended-text and ideographic WEA types. No credential was loaded and no host was contacted for this milestone. Query, transparent input, alternate-read, PF-mask, resequencing, and FET claims remain local-fixture claims until repeated against the IBM i release matrix; no compatibility claim is made yet for other structured fields, optional FCWs such as check digit, self-check, and cursor progression, extended text/ideographic attributes, fourteen-shade device rendering, DBCS, bidi, or a 5250 printer-device session.

## Local Forward Edge Trigger evidence

The Start-of-Field parser now recognizes exact FCW `X'8501'`, preserves it across field redefinition, and gives it precedence over the FFW Auto Enter flag. Typing the last data position or using a valid Field Exit, Field Plus, Field Minus, or Dup route produces a distinct typed FET outcome; the app then sends QSN_FET AID `X'50'` with the current modified-field payload. Mandatory-entry/fill checks remain Enter-like, and the OIA identifies the active contract as `FWD EDGE · AID FET`. Reviewed paste deliberately remains non-submitting and reports a staged FET field until an explicit Field Exit.

Six focused core tests cover parser precedence, FCW redefinition, every supported exit route, mandatory validation, exact AID/payload bytes, and non-submitting paste; one app test covers the 1,260×820 native OIA render. The complete suite executes 379 tests, skips one opt-in PUB400 probe, and reports zero failures. Pencil frame `SIjZs` reports no layout problems and its exported 2× preview is `Design/Previews/SIjZs.png`; the native render was inspected with the full field contract visible and unclipped. These are deterministic local fixtures: no credential, host connection, API key, or external provider was used, and live IBM i release-matrix qualification remains open.

## Local command-staging safety fixtures

The Host Command Deck is covered by model tests that prove a recognized command-entry field is cleared and replaced locally, while non-display fields reject staging. The app additionally requires a live, unlocked session, read-only classification, CCSID encoding, and enough field capacity before enabling the action. No AID key is generated by the staging path.

Rectangular terminal selection fixtures prove fixed-column text is preserved across rows and non-display cells are replaced with spaces before clipboard export. Selection is a local presentation feature and emits no host input.

Live PUB400 command staging and submission have not been exercised; authenticated host commands remain outside the current compatibility claim.

## Local display, language, and print evidence

The terminal now has separate local actions for PNG capture, a macOS PDF artifact, and the native print panel. All three blank non-display cells. PDF/print output includes the screen dimensions, local timestamp, and an explicit note that it generated neither host input nor an AI request. These actions are screen snapshots, not a claim of TN5250E printer-device support.

Fixtures prove both 24×80 and exact 27×132 presentation spaces. The native codec round-trips Hebrew CCSID 424, while the session profile correctly refuses it because byte conversion alone does not implement IBM's bidirectional field direction, cursor reversal, language layer, close, screen reverse, and OIA behavior. Mixed-byte 930/933/935/937/939 remain unselectable and fail explicitly; their shift-out/shift-in and dead-position semantics have not been implemented. No bidi, DBCS, physical print, or 5250 printer session was exercised against a host.

## Source & IFS evidence

The native source workspace now includes a typed system-SFTP IFS provider behind the explicit pinned-channel state. It provides:

- a real AppKit text surface with horizontal/vertical scrolling, line-number ruler, column guide, and live cursor position;
- atomic UTF-8 scratch autosave under the user's iTelAS Application Support directory with directory mode `0700` and file mode `0600`;
- typed local/member/IFS identities, absolute traversal-free IFS paths, format, CCSID, line-ending, source-date, and remote-revision metadata;
- deterministic draft-delta calculation; and
- bounded long-directory parsing and text downloads, strict UTF-8/CCSID 1208 decoding, SHA-256 revisions, a read-only conflict comparison, and a fail-closed staged-write review;
- staged and committed byte re-download checks, with no target deletion and no automatic replay after an uncertain result.

Four source-workspace model tests verify host identity rendering, scratch blocking, the complete ready path, and draft delta/byte counts. Nine IFS tests verify path normalization/traversal blocking and Codable invariants, full path/glob quoting without a shell, directory names with spaces, real empty directories and unknown-format rejection, CRLF round trips, binary/BOM/mixed-line-ending/CCSID rejection, strong revision-bound sibling plans, and hardened system-SFTP command construction. No IFS host browse, read, compare, rename, source-member read, compile, or host write was executed during this implementation milestone; the saved PUB400 credential was not loaded. The connected and conflict Pencil frame passes its static no-overflow check. Native inspection of the final signed arm64 bundle verified the default IFS selection, offline/pinned-channel gate, source editor accessibility value, path field, and secure-channel action without contacting a host.

## Source-member record evidence

The Source Member Record Workbench foundation uses Db2 record semantics rather than treating a QSYS source member as an SFTP text stream. It preserves `SRCSEQ`, `SRCDAT`, and `SRCDTA` as separate values, computes a canonical revision across metadata and ordered records, preserves unchanged dates, allocates insert sequence values only from real gaps, validates the declared EBCDIC CCSID and byte width, and requires journal before-and-after images for a write plan. Catalog search remains in SQL bindings, while validated object names are used only for a deterministic `QTEMP` member alias.

Fourteen focused tests cover identifier and Codable rejection, exact identity, fixed-precision sequence/date fields, revision sensitivity, changed-only date policy, insert gap allocation, no-gap/width/CCSID failures, journaling eligibility, safe SQL planning, declared-column decoding, malformed-schema refusal, and the editor bridge. The native workbench exposes a bounded three-stage catalog, record/date gutter, exact revision comparison, six visible gates, and a separate immutable reviewed-write sheet backed by the exact metadata/read/transaction protocol. No source-member catalog, read, alias, transaction, or host write was executed, and the saved PUB400 credential was not loaded. Pencil frame `a68oZ` reports no layout problems and its exported 2× preview is `Design/Previews/a68oZ.png`; write actions remain blocked without a qualifying, explicitly selected capability.

## Source Intelligence Desk evidence

The Source & IFS workspace now defaults to a switchable local Intelligence desk while preserving the existing Change Lens, record-integrity, and IFS write-flight-recorder controls. The desk shows format and dialect, a deterministic SHA-256 snapshot receipt, declaration outline, typed references, local structural checks, an explicit “not a compiler result” boundary, exact editor navigation, symbol-scoped Assist preparation, and a direct route to Compile Evidence. Re-analysis is debounced after edits and is recomputed synchronously when a different scratch, member, or IFS document becomes current. The native AppKit editor applies the current receipt's bounded semantic spans for directives, keywords, declarations, types, built-ins, string literals, numbers, and comments; stale spans are removed while analysis is pending, and attribute refresh preserves text, selection, typing attributes, undo history, and every write gate.

Nineteen focused tests cover first-line `**FREE` semantics; fully-free and column-limited RPG declarations, parameters, calls, `/COPY`, `/INCLUDE`, comments, and structural balance; CL variables, files, labels, programs, calls, and block comments; COBOL programs, divisions, sections, data items, paragraphs, COPY, and CALL; DDS formats, fields, keys, files, and referenced fields; qualified SQL object outline and the Db2 Studio boundary; deterministic receipts; input caps; control-character refusal; exact one-based source coordinates converted to UTF-16 selections including surrogate pairs; format-specific semantic vocabulary; quoted-comment delimiters; multiline CL and SQL comments including block-close precedence; declaration layering; deterministic nonduplicated directive spans; explicit color-span caps; and range bounds. The complete suite executes 279 tests with one opt-in PUB400 probe skipped and zero failures.

Pencil frame `Y9FsiR` reports no layout problems and its exported 2× preview is `Design/Previews/Y9FsiR.png`. The analyzer is capped at 2 MiB, 25,000 lines, 65,536 UTF-16 units per line, and 100,000 highlight spans. It does not expand includes, resolve catalog or binding identities, validate runtime objects, understand every release-specific dialect rule, compile source, read an API key, send AI context, contact PUB400, or perform a host operation.

## Source Completion Lens evidence

The native AppKit editor now exposes a caret-anchored completion lens through `Control-Space` and a visible editor-ruler action. It ranks qualified RPGLE fields and current-document fields, variables, parameters, data structures, prototypes, procedures, files, and formats before bounded format catalogs for RPGLE, CLLE, COBOL, DDS, and SQL. The session records the exact source SHA-256 receipt, UTF-16 caret, qualifier, prefix, and replacement range. Up/Down changes selection; Return or Tab applies one validated local insertion through the text system so undo remains available; Escape closes the lens; pointer activation uses the same validation path. Stale documents, invalid Unicode boundaries, recognized strings/comments, oversized prefixes, and analysis limits fail closed. The visually distinct Assist row never returns an insert edit; it opens the existing context dossier and sends nothing until the operator separately reviews and approves a request.

Eight focused tests cover qualified-field ranking and exact prefix replacement, current-document precedence, string/comment suppression, stale receipts, invalid surrogate positions, deterministic candidate and item caps, oversized-prefix refusal, undo-ready edit construction, non-inserting Assist actions, and stale apply rejection. The complete suite executes 279 tests with one opt-in PUB400 probe skipped and zero failures. Pencil frame `GZTJE` reports no layout problems and its exported 2× preview is `Design/Previews/GZTJE.png`. No host lookup, compiler invocation, API-key read, AI request, PUB400 authentication, source upload, or remote write was performed for this evidence.

## Db2 Query Flight Deck evidence

The Db2 studio now has a native connector boundary while remaining fail-closed on machines without its licensed runtime. It provides:

- six curated, bounded IBM i Services templates for active jobs, object locks, IFS locks, spooled output, system status, and ASP capacity;
- a native SQL editing surface with atomic local UTF-8 autosave;
- a comment/string-aware syntactic analyzer that rejects multiple statements, data-changing statements, DDL, administrative statements, `FOR UPDATE`, and data-changing table expressions;
- an execution gate for statement class, row cap, timeout, explicit non-production target, and provider availability; and
- a Pencil-first Connection Dossier with non-secret identity, device-only Keychain choice, fixed driver/runtime evidence, TLS/read-only policy, and sanitized state;
- a dynamic unixODBC actor that uses prepared statements, bounded rows/time/cells, typed values, and a non-secret capability receipt; and
- a live typed result plane that preserves nulls, decimals, date/time values, binary values, database types, and provenance.

The Typed Export Studio adds deterministic CSV/manifest and typed-JSON packages from an already-retained result. It preserves exact schema/types, nulls, decimals, temporal values, binary bytes, observed kinds, and query/result/schema/plan SHA-256 receipts; uses reversible spreadsheet-safe encodings; refuses missing provenance, unsafe destinations, symlinks, truncation, and resource-limit overruns; and saves only verified local artifacts. Its Assist pin carries schema and receipts only, never query text, target/provider identity, or cell values.

Six SQL model tests cover delimiter handling inside literals/comments, multi-statement and mutation rejection, CTE/data-change-table-expression behavior, all catalog bounds, offline/production fail-closed behavior, and a fully ready connected-development preflight. Four local Explain-review model tests cover deterministic clause stages, literal/comment isolation, policy-cap behavior, and statement-safety refusal; one app test covers the local-only review state and native rendering. Twelve additional Db2 tests cover profile/secret serialization boundaries, TLS/read-only connection keywords, diagnostic redaction, exact operation authorization, and failure before any driver/host operation when the runtime is absent. Four core export tests cover deterministic packages, exact encodings, bounds, receipt/provenance refusal, and safe replacement; four app tests cover retained-result lifecycle, schema-only Assist, save-panel gating, and native rendering. The complete suite executes 367 tests, skips the opt-in PUB400 probe, and reports zero failures.

No live Db2 connection, IBM i Service invocation, true asynchronous ODBC cancellation, or provider-backed optimizer explain plan has been exercised. The local Explain studio is a static, syntax-derived review that never executes SQL or contacts a provider; it cannot measure cardinality, selectivity, indexes, or access paths. The local machine lacks unixODBC and the IBM driver, so the dossier and transport stop before host contact; Build 41's export is entirely local. The templates and type mappings were checked against current official IBM documentation, but column availability can vary by IBM i release and group PTF level. Pencil frame `mB4on` reports no layout problems; its exported preview is `Design/Previews/mB4on.png`, and the native 1,180×760 render was inspected with the original Type Prism mark, English-only labels, English proverb, exact receipts, local save gate, and scrollable schema/contract rails unclipped.

## Local provider-readiness evidence

The System Provider Bay uses a typed fixed-path probe, not a host connection. On the development Mac it verified native `arm64`, `/usr/bin/ssh`, `/usr/bin/ssh-keyscan`, `/usr/bin/sftp`, and the Homebrew OpenSSL 3 library. unixODBC and the IBM i Access ODBC driver were absent, so Db2 remained visibly blocked. No package was installed, no host was contacted, and no credential source was read.

Three model tests prove that SSH/SFTP readiness does not accidentally enable Db2, that every Db2 runtime prerequisite is required, and that the candidate inventory contains only fixed noncredential paths. The Pencil frame passes its static no-overflow check; native runtime inspection verified the evidence rows and blocked Db2 state. The connection transport boundary test additionally proves this state fails before driver loading or host access.

## Secure Channel Dossier evidence

The dossier is implemented as an explicit trust workflow, not an automatic connection. Six model tests prove target/username option-injection rejection, expected-host-only key parsing and SHA-256 fingerprints, strict/no-forwarding SSH arguments with no shell, a fixed non-shell SFTP subsystem batch, idempotent `0600` known-host pinning, and refusal to overwrite a changed key for the same algorithm.

The native UI was inspected at its default sheet size and exposes every identity, host-key, capability, and blocked-state label through accessibility. The Pencil frame passes its static no-overflow check. No host-key scan, pin, SSH authentication, SFTP probe, IFS read, or host write was executed during this milestone; the saved PUB400 credential was not loaded.

## Assist Context and Proposal Dossier evidence

Source and SQL editors now report exact UTF-16 selection ranges to a dedicated dossier. The dossier can instead select the whole frozen draft, optionally capture one redacted terminal snapshot, and displays the exact text, bytes, provider host, model, document, SHA-256 baseline, and bundle fingerprint before send. The provider envelope escapes delimiter characters and labels every attached string as untrusted reference data.

Five focused tests cover Unicode/surrogate selection boundaries, line evidence, credential redaction, empty and per-item limit refusal, delimiter escaping, deterministic bundle fingerprints, duplicate-kind refusal, one-envelope proposal parsing, exact-document local selection replacement, ambiguous-envelope rejection, and document/baseline conflict refusal. The complete suite executes 279 tests with one opt-in PUB400 probe skipped and zero failures. The Pencil dossier frame reports no layout problems and its exported 2× preview is `Design/Previews/z3dHs.png`. No AI provider request, API-key read, host operation, query execution, source upload, or terminal action was performed for this evidence.

## Assist streaming and cancellation evidence

Ordinary assistant chat now requests an OpenAI-compatible Server-Sent Events response and renders only choice-zero content or refusal deltas as provisional text. Lines and events are independently capped at 128 KiB, accumulated output at 2 MiB, and completion requires `[DONE]` or a clean finish reason. Malformed JSON, truncation, overflow, non-success HTTP responses, and stale-generation callbacks fail closed. Stop immediately cancels the active task; received chat text is retained as explicitly stopped output with no completed-response command classification. The operator's next-question draft remains local. Source/SQL proposal review intentionally keeps the existing whole-response path, so cancellation cannot create a parsed or applicable partial proposal.

Eight app-target tests cover arbitrarily fragmented CRLF SSE, LF and multiline data, comments and metadata, refusal text, choice filtering, malformed/truncated/oversized streams, independent line/event/output bounds, `[DONE]` terminal behavior, request headers and `stream=true` body, incremental delta delivery, incomplete-turn exclusion from later conversation continuity, and separation of the test API key from the request body. The complete suite executes 279 tests with one opt-in PUB400 probe skipped and zero failures. Pencil frame `vw7wt` reports no layout problems and its exported 2× preview is `Design/Previews/vw7wt.png`; the native panel implements its provisional rail, channel instrument, original routed and Stop glyphs, preserved draft, and stopped/interrupted labels. Tests used an isolated in-process URL protocol and a non-secret literal key. No real provider, API-key read, host operation, PUB400 authentication, or external network request was performed for this evidence.

## Assist Context Shelf evidence

The Context Shelf composes explicitly pinned evidence from development, operations, and governance without giving Assist ambient workspace access. It preserves operator order, replaces an existing item of the same evidence kind in place, allows individual removal or a full local clear, and freezes a fresh deterministic bundle for each request. The exact preview exposes document identity, evidence kind, selection lines when present, redacted content, byte count, baseline and content fingerprints, destination, model, prior turns, total item/byte counts, and the untrusted-input boundary. Pinning or reviewing the shelf does not send a request.

Four focused tests prove pin/replace/remove/freeze behavior, the eight-item cap, accumulation across existing System Health and Compile workspaces, and visible refusal when optional automatic screen context would become a ninth item. The complete suite executes 279 tests with one opt-in PUB400 probe skipped and zero failures. Pencil frame `Hn6I6` reports no layout problems and its exported 2× preview is `Design/Previews/Hn6I6.png`; the native 1,260×790 SwiftUI sheet compiles with its original shelf-route and unpin glyphs, selected evidence rail, exact preview, byte budget, and send manifest. No provider request, API-key read, host operation, file write, query, compile, PUB400 authentication, or external network request was performed for this evidence.

## Immutable answer receipt evidence

Each completed, stopped, or interrupted Assist answer now retains the exact request receipt that was active for that response. A compact answer-level Receipt action opens a read-only inspector containing the submitted question, provider host, model, timestamp, prior-message count, exact redacted context fragments, bytes, redaction state, and fingerprints. Later shelf edits, previews, and sends cannot change an earlier answer receipt. The session ledger retains the 32 newest receipts, marks older answer rows with a disabled expired/session-limit badge, and clears with New Chat; the inspector cannot replay a request or trigger a provider or host action.

Five app-target tests prove distinct sequential receipts, deterministic oldest-entry pruning with explicit row expiry, stopped-answer inspection, sensitive-marker redaction retention, New Chat clearing, and native rendering at the 366×790 assistant-panel and 1,260×790 sheet sizes. The complete suite executes 372 tests, skips the opt-in PUB400 probe, and reports zero failures. Pencil frame `hQYpN` reports no layout problems and its exported 2× preview is `Design/Previews/hQYpN.png`; both native renders were inspected with available and expired receipt states, immutable/session labels, exact preview, manifest, submitted question, and no replay or clear-shelf control visible without clipping. Tests used deterministic local values only. No provider request, API-key or Keychain read, host operation, file write, query, compile, PUB400 authentication, or external network request was performed for this evidence.

## Proposal Patch Stack and Local Impact Lens evidence

Source and SQL proposals can now be queued as immutable alternatives without changing either editor. The stack permits at most twelve proposals, 262,144 UTF-8 replacement bytes, and 16,384 explanation bytes. Selected patches must share one target, document, baseline, and current draft; deterministic duplicates, invalid UTF-16 boundaries, overlapping hunks, stale or mixed baselines, and a whole-draft replacement combined with another patch fail before preview. The preview applies hunks in descending offset order and produces one exact prospective buffer.

Three focused core tests prove bounded collection, deterministic deduplication and reordering, compatible multi-hunk atomic preview, exact local impact evidence, and fail-closed overlap, stale-baseline, and whole-draft handling. Two app-target tests prove queueing without source mutation and native 1,320×820 rendering. The complete suite executes 279 tests with one opt-in PUB400 probe skipped and zero failures. Pencil frame `ILZOF` reports no layout problems and its exported 2× preview is `Design/Previews/ILZOF.png`; the native `NSHostingView` render was inspected at 1,320×820 with both selected hunks, exact before/after rows, local metrics, apply gates, and open evidence gaps visible.

The Local Impact Lens reports only exact byte, UTF-16, line, range, and affected-line changes. Compile status, host dependencies or SQL query plans, authority, and runtime behavior remain explicitly unverified. Queueing, ordering, selecting, previewing, removing, clearing, and applying read no API key and contact no provider or host. Apply rebuilds the preview and performs only one local Source or SQL buffer update; it does not save, upload, compile, query, collect host evidence, execute a command, or authenticate to PUB400.

## Compile Evidence Timeline evidence

The Build & Test tool now opens a native Compile Evidence Timeline instead of a capability placeholder. It includes three clearly labeled local replay runs, a searchable run ledger, exact recipe/event/job/source identities, source-linked rows, the full parsed diagnostic list, unlocated summaries, a command preview, an evidence-chain timeline, SHA-256 provenance, and a diagnostic dossier that labels primary ranking as inference.

Five focused tests prove direct `FILEID` identity and coordinates, long `FILEIDCONT` reconstruction, `EXPANSION` disclosure without false remapping, deterministic first-actionable ranking with an explicit non-causal contract, invalid-continuation refusal, and input caps. The Pencil frame `Z9QQ9u` reports no layout problems and its 2× preview is `Design/Previews/Z9QQ9u.png`.

The Compile Recipe Studio opens from this workspace with three visibly bundled examples until the operator explicitly saves a library. It supports typed RPGLE and SQLRPGLE program recipes, validates source and target system names, requires event evidence, exposes target-release, debug, commitment, preprocessing, environment, and replace choices, previews the exact generated command, and compares it with one separately retained run. Saving creates an atomic schema-v1 JSON library in a mode-`0700` Application Support directory and enforces mode `0600` on the file. Four focused core tests prove deterministic RPG and SQL command construction, target-release evidence, injection and inapplicable-option refusal, library bounds, identity/name uniqueness, and JSON round trips. Four app tests prove private custody and symlink refusal, model save/reload, visible run drift, and native 1,180×760 rendering.

Pencil frame `San1E` reports no layout problems and its exported 2× preview is `Design/Previews/San1E.png`. The native render was inspected at 1,180×760 with the original recipe-spindle and route marks, English-only labels and proverb, exact source/compiler/target route, typed options, command receipt, retained-run drift, local custody, and execution boundary unclipped.

The Compile Lineage Board compares only a chosen baseline/current pair within one exact target identity. It reports twelve exact/changed/relative/unavailable field states; introduced, resolved, and persistent message identities; a non-causal trend; and a deterministic receipt covering every scoped run and the chosen pair. Four core tests prove deterministic regression/recovery comparison, target-release states, exact diagnostic deltas, duplicate and cross-target refusal, and caps. Four app tests prove pair selection, no unrelated fallback, local Assist preparation, and native 1,180×760 rendering.

Pencil frame `COWVB` reports no layout problems and its exported 2× preview is `Design/Previews/COWVB.png`. The native render was inspected at 1,180×760 with original rail-switch and comparison-ledger marks, English-only labels and proverb, exact run route, field matrix, diagnostic delta, evidence boundary, and receipt unclipped.

No compiler command, event-file query, job-log query, source read, object replacement, API-key read, AI request, PUB400 authentication, or other host operation was performed. The bundled examples are synthetic local evidence. Recipe and lineage review have no provider or host path. Local imports are UTF-8-only, and complete `EXPANSION` mapping, remote collection, job-log correlation, recipe execution, compile outcome verification, causality, and runtime behavior remain outside this compatibility claim.

## Jobs & Queues Incident Thread evidence

The Operations tool now opens a native Incident Thread instead of a capability placeholder. Its deterministic local replay contains 23 jobs, queue aging and held/scheduled state, an exact qualified waiting job, a matching requested object lock, one exact-object holder candidate, an unrelated lock, five ordered job-log messages, one matching QSYSOPR inquiry, and four fingerprinted evidence receipts. The selected dossier states that exact-object matching is correlation rather than scheduler-causality proof, and lock rows are labeled “at snapshot” instead of receiving invented times.

The explicit live path reuses only the connected read-only ODBC capability and blocks production profiles. `JOB_INFO` is required; `OBJECT_LOCK_INFO`, exact-job `JOBLOG_INFO`, and QSYSOPR `MESSAGE_QUEUE_INFO` are collected independently so release or authority failures remain visible as sanitized gaps. Queries are capped at 250, 500, 500, and 100 rows with 30-second timeouts. Connection changes cancel collection and retain prior evidence without presenting it as current.

Seven focused tests prove ASCII qualified-job validation, single-statement read-only authorization for all four generated requests, column-order-independent decoding and queue summaries, duplicate/missing/malformed/over-cap refusal, exact-object-only holder correlation with non-causal wording, exact first/second-level message preservation and inquiry selection, control/text bounds, and the advice-only incident Assist context kind. The full suite passes 279 tests with one opt-in PUB400 probe skipped and zero failures.

The Pencil frame `W68mRI` reports no layout problems and its corrected 2× preview is `Design/Previews/W68mRI.png`. The native SwiftUI workspace compiles as part of the arm64 application. No Db2 connection, IBM i Service query, password read, API-key read, AI request, inquiry reply, job hold/release/end, PUB400 authentication, or other host operation was performed for this evidence.

## Spool & Output Document Inspector evidence

The Output tool now opens a native three-plane Document Inspector instead of a capability placeholder. Its deterministic local replay contains 96 files across four visible output queues, exact job/file/number/system identities, queue and writer pressure, statuses and print attributes, a whitespace-preserving ordered text preview, a separate exact-identity baseline, local comparison, and three evidence receipts. Search spans file, qualified job, user data, and output queue.

The live path deliberately separates passive inventory from content. An explicit non-production refresh issues bounded `QSYS2.SPOOLED_FILE_INFO` and optional `QSYS2.OUTPUT_QUEUE_INFO` requests without opening a file. A second explicit action binds one exact identity to bounded `SYSTOOLS.SPOOLED_FILE_DATA`; its receipt warns that IBM documents an internal spool-copy operation that may be audited. Inventory is capped at 250 rows, queues at 100 rows, text at 2,000 records of at most 378 characters, and every statement has a 30-second timeout.

Seven focused tests prove ASCII identity and positive-number validation, single-statement read-only authorization and fixed bounds, column-order-independent inventory decoding, queue/writer/authority decoding, record order and whitespace preservation, duplicate/control/cap refusal, exact-identity comparison with explicit non-lineage limits, and the advice-only spooled-output Assist kind. The full suite passes 279 tests with one opt-in PUB400 probe skipped and zero failures.

The Pencil frame `CMe1c` reports no layout problems and its refreshed 2× preview is `Design/Previews/CMe1c.png`. The native SwiftUI workspace compiles as part of the arm64 application. No Db2 connection, IBM i Service query, spooled-file content read, host spool-copy, password read, API-key read, AI request, hold, release, move, writer, print, send, delete, PUB400 authentication, or other host operation was performed for this evidence. Text preview and export do not claim page, font, overlay, graphic, AFP, IPDS, or printer-session fidelity.

## Data Transfer Integrity Lab evidence

The Transfer tool now opens a native three-plane Integrity Lab instead of a capability placeholder. Its deterministic replay profiles 1,284 CSV rows against an exact seven-column `ARLIB/CUSTOMER` target, preserves leading-zero customer and postal identifiers, identifies one ambiguous date and one CCSID 37 conversion failure as blockers, requests domain evidence for the observed `Y`/`N` token column, and records every blank as preserved text rather than silently treating it as NULL.

An operator may import a local UTF-8 CSV of at most 8 MiB, 25,000 data rows, 256 columns, and 65,536 bytes per cell. The RFC 4180-style parser accepts escaped quotes, CRLF/LF, and embedded line breaks in quoted fields; duplicate headers, malformed structure, controls, inconsistent rows, and resource-cap failures stop before a target query. Editing the target identity immediately makes prior schema evidence stale.

Live target discovery requires the existing connected, driver-enforced read-only Db2 capability and is blocked for production profiles. It issues one validated `QSYS2.SYSCOLUMNS2` statement with a 257-row overflow probe for the 256-column contract, decodes by unique column name, and refuses truncated, ambiguous, malformed, or duplicate-ordinal metadata. Schema receipts fingerprint the canonical returned metadata independently from the query fingerprint. The analyzer blocks unsupported/generated/hidden/non-updatable/field-procedure targets, blank scalar mappings without an explicit rule, leading-zero loss into numeric targets, range/precision/scale failure, truncation, ambiguous dates, and lossy CCSID conversion.

Ten focused tests prove quoted and multiline parsing, leading-zero preservation, structure and resource caps, validated single-statement read-only schema planning with overflow detection, column-order-independent metadata decoding, duplicate/missing/malformed/truncated refusal, distinct schema receipts, impossible-date rejection without calendar rollover, date and CCSID blockers, blank-scalar and leading-zero loss blockers, target truncation/decimal/required-column failures, and the advice-only transfer Assist kind. The complete suite passes 279 tests with one opt-in PUB400 probe skipped and zero failures.

The Pencil frame `CH3RH` reports no layout problems and its exported 2× preview is `Design/Previews/CH3RH.png`. The native SwiftUI workspace compiles as part of the arm64 application. No Db2 connection, target-data read, upload, IFS staging, insert, update, merge, create-table, API-key read, AI request, PUB400 authentication, or other host operation was performed for this evidence. Local validation reports and Assist context omit source cell values; host transfer execution is unavailable.

## System Health Evidence Cockpit evidence

The Health tool now opens a native evidence cockpit instead of a capability placeholder. Its deterministic local replay produces the designed 72/100 watch-state index from a 42.8% CPU sample, 78.4% system ASP use against an 85% threshold, 18,642 of 163,520 job-table entries, 42.6 GB temporary storage against a 61.3 GB peak, five pressure-ranked limit occurrences, and three installed-group PTF states. The local score exposes its exact penalty method and explicitly disclaims IBM ownership, diagnosis, trending, root cause, exhaustion timing, and outage prediction.

The explicit live path reuses only the connected driver-enforced read-only ODBC capability and blocks production profiles. It collects `SYSTEM_STATUS_INFO`, `SYSTEM_ACTIVITY_INFO(1)`, `ASP_INFO`, `SYSLIMITS`, and `GROUP_PTF_INFO` independently, with 30-second timeouts and caps of one singleton row, 64 ASPs, 100 limit occurrences, and 100 PTF groups. Column-order-independent decoders reject duplicate or missing columns, malformed values, unsafe text, impossible capacity relationships, singleton truncation, and over-cap results. Connection changes cancel collection and retain prior evidence without presenting it as current.

Ten focused tests prove generated single-statement read-only plans and bounds, deliberate absence of a certificate query, singleton decoding and ambiguity refusal, CPU sampling semantics, ASP sentinel/capacity handling, system-limit high-water evidence, PTF state decoding, transparent score calculation, export and Assist privacy boundaries, and malformed/over-cap refusal. The complete suite passes 279 tests with one opt-in PUB400 probe skipped and zero failures.

The Pencil frame `lOrLD` reports no layout problems and its exported 2× preview is `Design/Previews/lOrLD.png`. The native SwiftUI workspace compiles as part of the arm64 application. No Db2 connection, IBM i Service query, certificate-store access, password read, API-key read, AI request, PUB400 authentication, or host mutation was performed for this evidence. `CERTIFICATE_INFO` remains explicitly unavailable because password handling and elevated DCM authority require a separate capability review.

## Dependency and Impact Atlas evidence

The Dependency Atlas opens as a native evidence workbench with exact library/object/type controls, an original Impact Lattice instrument, a relation plane, edge ledger, decision dossier, source receipts, export, and reviewed Assist preparation. Its deterministic `ARLIB/ORDERSRV *SRVPGM` replay contains nine direct relations: seven `BOUND` ILE edges, one `CATALOG` SQL-routine edge, and one separately labeled `CANDIDATE` binding-directory entry. The assessment always remains `REVIEW REQUIRED`.

The explicit live path reuses only the connected driver-enforced read-only ODBC capability and blocks production profiles. It plans six independent 30-second requests across `OBJECT_STATISTICS`, `BOUND_SRVPGM_INFO`, `BOUND_MODULE_INFO`, `BINDING_DIRECTORY_INFO`, `SYSROUTINES`, and `SYSVIEWDEP`; object-type gates avoid irrelevant same-name catalog joins, and each source retains its own collected or unavailable receipt. Program-reference evidence remains a visible gap because `DSPPGMREF` requires a host output file and is deliberately never invoked.

Ten focused tests prove exact identity and object-type validation, six single-statement read-only bounded plans, deliberate absence of host writes, column-order-independent metadata and edge decoding, identity mismatch and duplicate/missing/malformed/truncated refusal, distinct `BOUND`/`CATALOG`/`CANDIDATE` semantics, unresolved `*LIBL`, safe deduplication, a permanent review-required verdict, local artifact limitations, and Assist identity withholding. The complete suite passes 279 tests with one opt-in PUB400 probe skipped and zero failures.

The Pencil frame `Rkk2f` reports no layout problems and its exported 2× preview is `Design/Previews/Rkk2f.png`. The native SwiftUI workspace compiles as part of the arm64 application. No Db2 connection, IBM i Service query, `DSPPGMREF`, output-file creation, password read, API-key read, AI request, PUB400 authentication, or host mutation was performed for this evidence. Live release and authority coverage, runtime frequency, transitive completeness, and safe-change conclusions remain outside this claim.

## Runbook Flight Deck evidence

The Runbooks tool now opens a native adaptive Flight Deck instead of a capability placeholder. Its deterministic `COMPILE_VERIFY` replay resolves eight typed steps for the exact `DEV ORION` target: local assertions, two bounded read-only SQL checks, one classified `CRTBNDRPG` preview, a two-role review gate, and explicit compile and object-signature evidence requirements. The visible execution map and dossier always report `REVIEW REQUIRED`; the connector control is locked.

Local JSON import uses a 128 KiB bounded read before decoding and permits at most 32 contiguous steps, 24 typed parameters, 4 KiB of UTF-8 per action template, and bounded single-line values. Resolution rejects secret-like parameter names, unknown or malformed placeholders, free-text action interpolation, unsafe SQL, multiline or command-separated CL, disallowed environments, mutation-budget overflow, duplicate approvals, and stale approval fingerprints. Byte-length-framed fingerprints change when blueprint metadata, resolved steps, evidence, or approval-role contracts change even if an imported revision number is reused. Changing the target, environment, or typed value invalidates the plan and clears local attestations. Export prefixes every action-preview line and states its limitations; Assist context withholds identities, values, actions, fingerprints, and timestamps.

Eleven focused tests prove secret-parameter refusal, typed normalization and injection resistance, structural and placeholder validation, deterministic exact resolution, read-only SQL enforcement, CL separator and mutation-budget gates, plan-bound local attestations, permanent non-executability, destructive and unknown blocking, exact bounded artifacts, Assist privacy, and codec round trips and limits. The native SwiftUI target also compiles with the routed Flight Deck and its local import, review, copy, export, and Assist preparation controls.

The complete suite passes 279 tests with one opt-in PUB400 probe skipped and zero failures.

The Pencil frame `KWrr3` reports no layout problems and its exported 2× preview is `Design/Previews/KWrr3.png`. No command, SQL statement, approval, schedule, remote runner, password read, API-key read, AI request, PUB400 authentication, or host operation was performed for this evidence. Local attestations are explicitly not authenticated signatures, evidence entries are requirements rather than collected proof, and the implementation contains no runbook executor or automatic resume path.

## Authority Path Atlas evidence

The Security Advisor now opens a native Authority Path Atlas instead of a capability placeholder. Its deterministic `RPGDEV1` and `ARLIB/PAYROLL *FILE` replay presents six explained static paths: four report access and two retain explicit public exclusions. Three runtime authority-check observations remain visually and semantically separate from those static rows. Five source receipts are collected and the intentionally unavailable complete-effective-resolution source keeps the assessment at `REVIEW REQUIRED`.

The explicit live path reuses only the connected driver-enforced read-only ODBC capability and blocks production profiles. It plans four independent 30-second queries across `USER_INFO`, `GROUP_PROFILE_ENTRIES`, `OBJECT_PRIVILEGES`, and `AUTHORITY_COLLECTION`, then issues at most one `AUTHORIZATION_LIST_USER_INFO` query only when one caller-visible authorization-list identity was decoded. Overflow probes enforce caps of 16 memberships, 100 object grants, 100 authorization-list entries, and 100 observations. Column-order-independent decoders reject duplicate/missing columns, identity mismatches, unsafe controls, malformed authority indicators, truncation, and over-cap results. A missing authorization-list identity remains a gap instead of being inferred as absence.

Eleven focused tests prove classic profile and object identity gates, five generated single-statement read-only plans and fixed bounds, absence of password fields and mutation statements, column-order-independent decoding, duplicate/missing/malformed/mismatched/truncated refusal, detailed authority-bit preservation, primary-group authorization-list recognition, separate static/observed/adopted-authority semantics, local no-write simulation, exact artifact output, and Assist identity withholding. The complete suite passes 279 tests with one opt-in PUB400 probe skipped and zero failures.

The Pencil frame `uRLmx` reports no layout problems and its exported preview is `Design/Previews/uRLmx.png`. The native SwiftUI workspace compiles with its custom route marks, evidence ledger, path lattice, authority matrix, dossier, local what-if, export, and Assist preparation controls. No Db2 connection, IBM i Service query, password read, API-key read, AI request, grant, revoke, profile change, authorization-list change, authority-collection change, PUB400 authentication, or other host operation was performed for this evidence. Finder/Dock icon rendering remains deferred until an unlocked desktop session is available.

## Continuity Casebook evidence

The Continuity Casebook opens as a native handoff workspace with a local case queue, dominant-question header, chronological evidence relay, open-question and stale-evidence boundary, six custody gates, Assist answer ledger, reviewed-reference controls, and immutable snapshot manifest. Its deterministic replay covers incident, change, build, and transfer cases without implying current host state. Creating or capturing a case switches to a durable local store; replay content is never silently persisted.

Five focused core tests prove exact artifact, Assist-answer, reviewed-reference, and immutable-snapshot collection; deterministic deduplication and bounds; stopped-output risk-label refusal; traversal-safe local reference packs with explicit entry selection; persistence/tamper validation; and exact redacted Context Shelf capture. Five app-target tests prove `0700`/`0600` store permissions, broadly readable path refusal, parent and leaf symlink refusal, millisecond-exact durable round trips, explicit current-context capture, completion/request provenance retention, explicit reviewed-entry advice-only pinning, and native 1,360×840 rendering. The complete suite passes 279 tests with one opt-in PUB400 probe skipped and zero failures.

Pencil frame `eoLjn` reports no layout problems and its exported 2× preview is `Design/Previews/eoLjn.png`. The native render was inspected at 1,360×840 with the original Relay Knot mark, Casebook workbench glyph, English-only labels and proverb, visible replay/local/provider/host boundaries, evidence relay, answer provenance, reviewed-reference entry controls, and snapshot actions unclipped. An independent criteria-bound review passed persistence/immutability, explicit no-ambient Assist flows, and visual/local-boundary criteria. No API key, Keychain item, provider request, network connection, IBM i query, host action, PUB400 authentication, repository scan, or runbook-directory scan was performed for this evidence. Exports remain sensitive local custody artifacts rather than authenticated approvals.

## Session Flight Recorder evidence

The native Session Flight Recorder retains host screens only after the operator arms one exact session or bookmarks a screen. Its redaction pass clears every input-field range, all non-display cells, and complete sensitive-labeled rows before computing an exact display-only fingerprint. Duplicate newest frames collapse; 7/30/90-day retention and a 250-frame cap prune the archive. Local reconstruction contains no editable fields or recoverable input. The `terminal-flight-recorder-v1.json` store uses atomic writes, a 12 MiB cap, `0700`/`0600` permissions, and refuses broadly readable, parent-symlinked, leaf-symlinked, nonregular, oversized, or fingerprint-invalid custody.

Reviewed macros contain bounded exact-screen matches, read-only command staging, field actions, whitelisted host keys, and evidence bookmarks. Editing invalidates review; common secret assignments and unsupported AIDs fail validation; a configured profile binding must match; and live command staging still passes the conservative command classifier and recognized visible field gate. One explicit operator action evaluates one step, records a passed or blocked receipt, and pauses. No background worker, host event, retry, or reconnect path can advance a route.

Five focused core tests prove complete input/non-display/sensitive-row clearing, display-only reconstruction, fingerprint validation, duplicate and retention bounds, exact-review invalidation, secret/AID/step-limit refusal, receipt round trips, and tamper detection. Three app-target tests prove restricted permissions, broad-permission refusal, parent and leaf symlink refusal, explicit bookmark persistence, reviewed single-match-step execution, durable receipts, and native 1,360×840 rendering. The complete suite passes 287 tests with one opt-in PUB400 probe skipped and zero failures.

Pencil frame `w540sM` reports no layout problems and its exported 2× preview is `Design/Previews/w540sM.png`. The native render was inspected at 1,360×840 with the original route/tape mark, English-only labels and proverb, visible local-replay and custody boundaries, durable tape, dominant redacted terminal evidence, exact receipt, reviewed macro route, and session-required execution gate unclipped. No Keychain item, password, API key, provider request, network connection, IBM i command, AID, PUB400 authentication, or host action was used for this evidence. Local macro review is not an authenticated approval, protected host output may still be sensitive, and live interoperability remains part of the supported-release compatibility matrix.

## Source Cross-Reference Atlas evidence

The native Source Cross-Reference Atlas opens from the Source & IFS workbench and indexes only an explicitly selected local folder. Its in-memory document ledger covers supported RPGLE, CLLE, COBOL, DDS, SQL, and text extensions; workspace search distinguishes file, symbol, reference, and text matches. A same-root refresh rereads every supported file byte, reuses analysis only after an exact path/format/UTF-8 match, rebuilds every current dependency edge, and publishes a deterministic before/current index-bound ledger for reused, reanalyzed, metadata-only, added, and removed files. The Workspace Drift Radar starts with the selected root and recursively monitors supported paths through FSEvents; a bounded metadata-only scanner provides a session fallback when XCTest or macOS cannot start that stream. Signals are normalized, safely rooted, coalesced over 650 milliseconds, bounded to 2,048 raw observations and 256 unique paths, and fingerprinted without source content or timestamps. Dropped, wrapped, root, overflow, pause, and rescan events require full exact verification. Pending drift immediately revokes dependency completion, include mapping, compiler attachment/navigation, rename application, opened-snapshot currency, and occurrence navigation. A refresh clears only event identifiers at or before its starting boundary, so later changes remain pending for another scan. Automatic refresh, pause/resume, and Verify now remain explicit, session-only controls. The ledger and source remain memory-only. Nonempty queries debounce for 140 milliseconds, run off the main actor, respond to cancellation during document and line traversal, reject stale task/index/query generations, and publish exact examined-file/line, candidate, result-cap, completion, duration, and deterministic receipt evidence. An occurrence opens as a stale-aware read-only editor snapshot; dependency edges stay labeled exact, ambiguous, host content not loaded, or unresolved. The Include Chain Navigator recursively traverses only exact indexed `/COPY` and `/INCLUDE` targets and binds a deterministic receipt to the current index, root and document fingerprints, directive ranges, route provenance, candidates, limits, and visible boundaries. Shared exact targets are retained without repeated traversal; ambiguity, absent host bytes, unresolved targets, cycles, depth, document, and directive caps stop visibly. Its exact closure can stage the draft completion selection, while approval remains a separate review. It does not evaluate conditional compilation, SQL preprocessing, compiler expansion, binding, or release-specific semantics. One exact host-backed `/COPY` or `/INCLUDE` edge may enter an index/source/target/provider-bound review, require explicit attestation, and perform a single existing-provider source-member or IFS read. Exact returned identity/revision/CCSID evidence installs a bounded provenance-marked in-memory overlay that participates in search, dependency resolution, recursive include mapping, and reviewed completion. It never falls back to another library/path, persists content, writes to the host, or sends content to Assist. Only an exact reviewed path/content/index receipt exposes cross-file symbols to editor completion. The Compiler Evidence Bridge maps retained source-member, IFS, relative-path, or base-name file identities into current indexed documents through an explicit index/run/evidence-bound review. It retains `TGTRLS` and observed-host release context, distinguishes exact/changed/unrecorded source revisions, and exposes exact navigation only when coordinates are in bounds and no `EXPANSION` record blocks source remapping. Attachment requires attestation and remains local; the bridge performs no compile, provider, host, Keychain, or Assist action. Rename excludes host overlays, freezes exact local token ranges and baselines, opens a separate immutable manifest and changeset, requires checkbox attestation, validates the full batch before writing, uses verified atomic per-file replacement with permission preservation, rolls earlier replacements back after failure, and rebuilds the index before reporting success. Replay can install only bundled deterministic overlay content and cannot Apply a rename. The multi-file batch is explicitly not crash-atomic.

Seventeen focused core tests prove traversal and duplicate-path refusal, file and search bounds, deterministic indexing, all four search kinds, all four dependency-resolution states, exact reviewed completion scope, whole-index review invalidation, comment/string-aware rename ranges and replacements, immutable review invalidation, limited-analysis refusal, source-date/modification/content baselines, stale-plan detection, occurrence caps, exact host-edge and explicit-library review, source-member and IFS identity/revision/CCSID matching, workspace-budget enforcement, overlay provenance/fingerprinting/resolution, host exclusion from rename, local-only overlay removal, query/index-bound search receipts, disclosed coverage caps, cooperative cancellation, exact-byte-only analysis reuse, deterministic delta receipts, dependency rebuilding, cancellation, a 750-file unchanged reuse path, safe drift-path refusal, deterministic case-insensitive signal coalescing, raw/unique-event caps, rescan escalation, and scan-boundary retention. Four Include Chain tests prove recursive exact closure, shared-route retention, deterministic receipts, explicit cycle/ambiguity/host boundaries, and fail-closed document, directive, and depth limits. Six additional bridge tests prove exact/relative/missing target-release context, conflicting-token refusal, local/member/IFS mapping rank, index/run/evidence/revision binding, exact-only navigation, visible changed/unrecorded/expansion/out-of-bounds evidence, duplicate-target refusal, and stale-review detection. Fourteen Atlas app tests plus three bridge app tests prove explicit-folder scanning, replay and rename gates, host-overlay behavior, exact local attachment, exact snapshot opening, changed-revision blocking, debounced newest-generation publication, current delta-receipt publication, exact-byte scanner reuse, safe drift-path mapping, immediate stale-gate revocation, pause/resume, manual and automatic verification, metadata fallback, and native rendering. Two Include Navigator app tests prove exact-current closure staging, drift revocation, and native rendering. The complete suite passes 357 tests with one opt-in PUB400 probe skipped and zero failures.

Pencil frames `Y55H1`, `c2uSHo`, `w1bowr`, `b0hmA6`, `ZOl1b`, `IFgqW`, `xg5n3`, `IRzvb`, `San1E`, and `COWVB` report no layout problems; their exported previews are in `Design/Previews/`. Native renders were inspected at 1,360×840, 1,180×760, and 1,080×720 with English-only labels and proverbs, original source-route, responsive-search, incremental-refresh, drift-pulse, signal-circuit, compiler-bridge, include-loom, recipe-spindle, lineage rail-switch, comparison-ledger, mapping, rename-batch, host-relay, document, and shield glyphs, visible local/provider/host boundaries, exact search, delta, signal, include, recipe, and lineage receipts, immutable manifests, recovery caveats, replay lockout, and attestation gates unclipped. Tests wrote only generated temporary source fixtures and installed only bundled in-memory host replay content. No user source folder, API key, Keychain item, provider, network, compiler, IBM i host, or PUB400 credential was read or used.

## Native bundle and icon evidence

Development bundle `0.1.0 (41)` contains an ad-hoc-signed thin arm64 Mach-O executable and a valid English `Info.plist`. Its primary `CFBundleIconName` is `AppIcon`, compiled by Apple's asset-catalog tool from all ten required macOS 1x/2x sizes into `Assets.car`; `CFBundleIconFile=iTelAS.icns` remains the compatibility fallback. The packaged 1024-pixel PNG and ICNS are byte-identical to their sources, and direct AppKit loading of the fallback returns all ten 16-through-1024-pixel renditions. Native transparent-bitmap rendering shows the complete original blue, mint, white, and dark mark from both packaged formats. The app deliberately installs the PNG as its runtime application icon before falling back to the ICNS. The build now fails if either rendered format is visually blank, flat, missing its transparent corner silhouette, missing required ICNS sizes, or if the asset catalog, plist declarations, or packaged resources are absent or inconsistent. Swift Package runs retain the identical PNG resource. The restricted verification process returns the same generic placeholder for built-in macOS apps, so Finder/Dock cache behavior still requires a live unlocked-session check; public distribution still requires Developer ID signing and notarization.
